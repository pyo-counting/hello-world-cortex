## Prometheus
# Prometheus config file doesn't support environment variable exapnsion.
# Only supported 'external_labels' config when it started with '--enable-feature=expand-external-labels' flag

### global
#### external_labels:
cluster=prd

### remote_write
#url=
#### headers
#XScopeOrgID=